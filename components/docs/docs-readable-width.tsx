/** Readable-width shell for documentation and tool pages (not the homepage hero). */
export const DocsReadableWidth = ({
  children,
}: {
  children: React.ReactNode
}) => <div className="mx-auto w-full max-w-5xl">{children}</div>
