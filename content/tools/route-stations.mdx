## Route stations

Stop sequence for one line and direction (`?lineId=&direction=`). Linked from Browse lines and the line diagram live demos.

This is a site tool — not an installable registry item.
