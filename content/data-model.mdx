import { NetworkModelDiagram } from "@/components/docs/network-model-diagram"
import {
  NetworkModelStatusLegend,
  NetworkModelStatusTable,
} from "@/components/docs/network-model-status-table"

Carriage diagrams, platform diagrams, the Tube map, and the geographic map share one passenger network: lines, stations, the patterns that run on them, and — for the geographic map — the tracks those patterns follow.

- **TfL** says which stations a service calls at, in what order. That is enough for the carriage map, the platform map, and the Tube map.
- **OpenStreetMap** says which metres of track a service runs on. That is what the geographic map draws, once a pattern is joined to a route by station membership rather than by snapping a station onto the nearest track.
- **MapProductPolicy** is authored here. No feed decides which patterns belong on a carriage map, a platform map, the Tube map, or the geographic map.

A public timetable is not required to draw these four maps. How often a pattern runs does not change station order or track.

Station name labels are a separate concern.

## The model

<NetworkModelDiagram />

In-carriage and platform maps use the same records. Their difference is layout. The Tube map keeps the usual branches. The geographic map draws the tracks.

## Status

<NetworkModelStatusLegend />

<NetworkModelStatusTable />

## Where it comes from

Line identity and station order come from TfL. Track geometry comes from OpenStreetMap, simplified for a map and joined to a pattern by matching the ordered stations on an OSM route relation. Inspect a line on [Line topology](/docs/line-topology).

| Source | Covers | When to use it |
|---|---|---|
| [TfL Unified API](https://api-portal.tfl.gov.uk/) via `tfl-ts` | Lines, stations, hubs, arrivals, status | Everyday boards and the three schematic maps. Free key. |
| [TfL station-data GTFS](https://api.tfl.gov.uk/stationdata/tfl-stationdata-gtfs.zip) | Stations, platforms, hubs, step-free pathways | Interchange grouping and platform identity. No timetables. |
| OpenStreetMap | Track geometry | Geographic maps. Already the basis of the simplified unique-track set. |

## Next

1. Join Underground, DLR, and Tram patterns to OSM route relations by ordered station membership, replacing nearest-vertex snapping. This is what the geographic map needs to draw a branch correctly at a fork.
2. Author `MapProductPolicy` for the four maps — which patterns belong on a carriage diagram, a platform diagram, the Tube map, and the geographic map.
3. Point the geographic map and the line / branch strips at this model.

See [Map – Tube & Rail (Geo)](/docs/map-geographic), [Simple line strip](/docs/line-strip), [Branch strip — horizontal](/docs/branch-strip-horizontal), and [Branch strip — vertical](/docs/branch-strip-vertical).
