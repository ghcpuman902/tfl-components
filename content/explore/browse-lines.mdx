## Browse lines

Lists tube and rail lines by mode and links into **Route stations** for a stop sequence. This is a site tool — not an installable registry item.

Uses a cached `line.get({ modes })` call covering tube, Elizabeth line, DLR, Overground, and tram.
